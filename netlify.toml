[build]
  command = "npm run build"
  publish = "dist"

[dev]
  port = 3006
  framework = "#auto"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
